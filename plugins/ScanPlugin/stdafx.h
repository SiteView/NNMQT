// stdafx.h : 标准系统包含文件的包含文件，
// 或是经常使用但不常更改的
// 特定于项目的包含文件
//

/*
#pragma once

#pragma warning(disable: 4819)

#pragma comment(lib, "ws2_32.lib")

#pragma comment(lib, "netapi32.lib") // netapi32.dll是Windows网络应用程序接口，用于支持访问微软网络。

//#pragma comment(lib, "snmp_pp.lib")
#pragma comment(lib, "snmp_++.lib")

#pragma comment(lib, "libdes.lib")

//#pragma comment(lib, "Topology.lib")
#ifdef _DEBUG
#pragma comment(lib, "iced.lib")
#pragma comment(lib, "iceutild.lib")
#else
#pragma comment(lib, "ice.lib")
#pragma comment(lib, "iceutil.lib")
#endif


#define TOPOLOGY_BUILD_AS_DLL

#ifndef WINVER				// 允许使用特定于 Windows XP 或更高版本的功能。
#define WINVER 0x0501		// 将此值更改为相应的值，以适用于 Windows 的其他版本。
#endif

#ifndef _WIN32_WINNT		// 允许使用特定于 Windows XP 或更高版本的功能。
#define _WIN32_WINNT 0x0501	// 将此值更改为相应的值，以适用于 Windows 的其他版本。
#endif						

#ifndef _WIN32_WINDOWS		// 允许使用特定于 Windows 98 或更高版本的功能。
#define _WIN32_WINDOWS 0x0410 // 将此值更改为适当的值，以指定将 Windows Me 或更高版本作为目标。
#endif

#ifndef _WIN32_IE			// 允许使用特定于 IE 6.0 或更高版本的功能。
#define _WIN32_IE 0x0600	// 将此值更改为相应的值，以适用于 IE 的其他版本。
#endif

#define WIN32_LEAN_AND_MEAN		// 从 Windows 头中排除极少使用的资料

// #include <stdio.h>
// #include <tchar.h>
// #include <Windows.h>
// #include <WinSock2.h>
#include <atlbase.h>
#include <atlcom.h>
 */
//#include <stdio.h>

// TODO: 在此处引用程序需要的其他头文件
